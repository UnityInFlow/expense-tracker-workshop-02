package dev.workshop.expense

// STEP 6 — Error handling: a dedicated exception for "expense not found", thrown by the
// controller and translated to a 404 by GlobalExceptionHandler.
//
// This file is intentionally empty so the Session-1 app still compiles. Fill it in during STEP 6.
//
// TODO (step 6): declare the exception:
//   class ExpenseNotFoundException(id: Int) : RuntimeException("Expense with ID $id not found")
