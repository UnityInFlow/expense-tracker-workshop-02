server:
  port: 8080

# STEP 1 — SQLite setup: connect Spring to a SQLite file and run schema.sql on startup.
spring:
  datasource:
    url: jdbc:sqlite:expenses.db
    driver-class-name: org.sqlite.JDBC
  sql:
    init:
      mode: always
