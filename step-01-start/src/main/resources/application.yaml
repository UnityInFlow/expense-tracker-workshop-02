# ═══ STEP 1 — SQLite setup & schema ═══  Fill in the TODO blocks below, then run: ./mvnw spring-boot:run   (solution: ../step-01-final/)
server:
  port: 8080

# TODO (1.1): Uncomment and complete the Spring datasource block below so the app connects to SQLite
#   Hint: remove the leading '#' from every line, keep the indentation as-is
# spring:
#   datasource:
#     url: jdbc:sqlite:expenses.db
#     driver-class-name: org.sqlite.JDBC
#   sql:
#     init:
#       mode: always
