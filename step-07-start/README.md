# Step 7 — OpenAPI finalization

## What we already have
- Steps 1-6: Complete Expense Tracker with SQLite, Repository, Validation, Error handling

## What we will do
Improve the Swagger UI documentation using @Schema annotations.
Production-ready API documentation.

## Tasks
1. Add @Schema annotations to Expense.kt (see TODO)
2. Add @Schema annotations to CreateExpenseRequest.kt
3. Add @ApiResponse annotations to methods in ExpenseController.kt
4. Verify in Swagger UI: http://localhost:8080/swagger-ui.html
